# ═══════════════════════════════════════════════════════
# Site Monitor - Python 依赖
# ═══════════════════════════════════════════════════════

# HTTP 客户端 (比 requests 更现代, 支持 HTTP/2)
httpx[http2]==0.28.*

# TLS 证书解析 (cryptography 比 ssl 更强大)
cryptography==44.*

# 配置校验
pydantic==2.*

# ── 可选依赖 (按需安装) ─────────────────────────────────────
# Prometheus 指标采集 (如需推送模式)
# prometheus-client==0.*

# ── 开发依赖 (本地测试用) ───────────────────────────────────
# pytest==8.*
